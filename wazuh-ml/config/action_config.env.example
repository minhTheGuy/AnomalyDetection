# Action Configuration Environment Variables
# Copy this file to .env or export these variables in your shell
# All scripts automatically load this file using python-dotenv

# ============================================================================
# pfSense Configuration
# ============================================================================
# pfSense firewall connection settings for packet capture
PFSENSE_HOST=172.16.158.100
PFSENSE_USER=admin
PFSENSE_PORT=22
PFSENSE_INTERFACE=em1
PFSENSE_KEY_FILE=~/.ssh/id_rsa

# ============================================================================
# Detection Settings
# ============================================================================
# Capture interval in seconds (for continuous mode)
DETECTION_INTERVAL=15

# Detection threshold (0.0-1.0, higher = more sensitive)
DETECTION_THRESHOLD=0.5

# Block duration in seconds (for auto-blocking)
BLOCK_DURATION=300

# Whitelist IPs (comma-separated, these will NEVER be blocked)
WHITELIST_IPS=172.16.158.1,172.16.158.100

# ============================================================================
# Webhook Configuration
# ============================================================================
# Webhook URL for sending detection alerts via HTTP POST
# Required when using --action webhook
# WEBHOOK_URL=https://localhost:5000/webhook

# ============================================================================
# Email Configuration
# ============================================================================
# SMTP server for sending email alerts (CRITICAL threats only)
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=587
# EMAIL_USERNAME=
# EMAIL_PASSWORD=
# EMAIL_FROM=
# EMAIL_TO=

# Example for Gmail:
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=587
# EMAIL_USERNAME=your-email@gmail.com
# EMAIL_PASSWORD=xxxx xxxx xxxx xxxx  # Gmail App Password (16 chars)
# EMAIL_FROM=your-email@gmail.com
# EMAIL_TO=admin@example.com

# Example for local SMTP (Postfix/Sendmail):
# EMAIL_SMTP_HOST=localhost
# EMAIL_SMTP_PORT=25
# EMAIL_FROM=nids@localhost
# EMAIL_TO=admin@localhost

# ============================================================================
# Wazuh Configuration
# ============================================================================
# Wazuh Manager API configuration
WAZUH_MANAGER_HOST=172.16.158.150
WAZUH_MANAGER_PORT=55000
WAZUH_API_URL=https://172.16.158.150
WAZUH_API_USER=
WAZUH_API_PASSWORD=

# Wazuh Socket Path (optional, auto-detected if not specified)
# Common paths:
# - /var/ossec/queue/sockets/com
# - /var/ossec/queue/alerts/socket
# WAZUH_SOCKET_PATH=/var/ossec/queue/sockets/com

# Wazuh Connection Method (auto, socket, api, log)
# - auto: Automatically detect best method
# - socket: Use Wazuh socket (fastest, requires socket file)
# - api: Use REST API (requires credentials)
# - log: Write to log file (file-based integration)
# WAZUH_METHOD=auto

